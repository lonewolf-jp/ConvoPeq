# 実装チェックリスト — 測定項目追加計画書 第9版

> 作成日: 2026-06-28
> 状態: 実装完了
> リンク: [測定項目追加計画書](./測定項目追加計画書.md)

---

## Step 0: DiagnosticsConfig.h 共通基盤追加 ✅

| # | タスク | ファイル | 状態 | 備考 |
|---|-------|---------|------|------|
| 0.1 | `ProcessMemoryInfo` 構造体追加（privateUsage/workingSet/pageFaultCount） | `DiagnosticsConfig.h` | ✅ | |
| 0.2 | `getProcessMemoryInfo()` 関数追加（GetProcessMemoryInfoラッパー） | `DiagnosticsConfig.h` | ✅ | |
| 0.3 | `diagSequenceCounter()` 追加（全ログ共通の単調増加カウンタ） | `DiagnosticsConfig.h` | ✅ | |
| 0.4 | `updateAtomicMaximum()` ヘルパー追加（compare_exchange_weak隠蔽） | `DiagnosticsConfig.h` | ✅ | |
| 0.5 | `#pragma comment(lib, "psapi.lib")` 削除（cpp側へ移動） | `DiagnosticsConfig.h` | ✅ | |
| 0.6 | 重複定義削除: Timer.cppのlocal ProcessMemoryInfo/getProcessMemoryInfo()削除 | `Timer.cpp` | ✅ | 共通定義を利用 |
| 0.7 | 重複定義削除: Commit.cppのlocal ProcessMemoryInfo/getProcessMemoryInfo()削除 | `Commit.cpp` | ✅ | 共通定義を利用 |

## Step 1: Audio callback 1秒サマリ（CBSUMMARY） ✅

| # | タスク | ファイル | 状態 | 備考 |
|---|-------|---------|------|------|
| 1.1 | `intervalMaxUs_` 追加（atomic<uint32_t>） | `AudioEngine.h` | ✅ | ★主指標 |
| 1.2 | `callbackMaxUs_` 追加（atomic<uint32_t>） | `AudioEngine.h` | ✅ | ★副指標 |
| 1.3 | `callbackCount_` 追加（atomic<uint32_t>） | `AudioEngine.h` | ✅ | 参考値 |
| 1.4 | CBSUMMARY出力ブロック追加（Timer.cpp、1秒1回・変化時のみ） | `Timer.cpp` | ✅ | intervalMax主指標 |
| 1.5 | `updateAtomicMaximum()` 呼び出し（callback末尾） | `AudioBlock.cpp` | ✅ | RT-safe: relaxed ordering + compare_exchange_weak |
| 1.6 | `callbackCount_.fetch_add(1)` 呼び出し（callback末尾） | `AudioBlock.cpp` | ✅ | |

## Step 2: PageFault Delta + 2条件警告 ✅

| # | タスク | ファイル | 状態 | 備考 |
|---|-------|---------|------|------|
| 2.1 | MEMログにSeq=とDelta=追記 | `Timer.cpp` | ✅ | |
| 2.2 | EWMA計算（alpha=0.05、判定→更新順） | `Timer.cpp` | ✅ | |
| 2.3 | EWMAクリッピング（min(delta,50000)） | `Timer.cpp` | ✅ | ベースライン汚染防止 |
| 2.4 | EWMA warmup（10サンプル） | `Timer.cpp` | ✅ | 初回はabsolute判定のみ |
| 2.5 | 警告出力（absolute OR relative） | `Timer.cpp` | ✅ | |

## Step 3: Obsolete ビルド浪費検出 ✅

| # | タスク | ファイル | 状態 | 備考 |
|---|-------|---------|------|------|
| 3.1 | isObsolete(): 初回continue前 ログ追加（currentGen付き） | `RebuildDispatch.cpp` | ✅ | lastCommittedRuntimeGeneration_読取 |
| 3.2 | isObsolete(): prepare完了後 ログ追加（phase=prepare/currentGen/wastedMs） | `RebuildDispatch.cpp` | ✅ | |
| 3.3 | isObsolete(): rebuildIR前 ログ追加（phase=rebuildIR/currentGen/wastedMs） | `RebuildDispatch.cpp` | ✅ | |
| 3.4 | isObsolete(): warmup後 ログ追加（phase=warmup/currentGen/wastedMs） | `RebuildDispatch.cpp` | ✅ | |

## Step 4: Timer jitter + 実行時間 ✅

| # | タスク | ファイル | 状態 | 備考 |
|---|-------|---------|------|------|
| 4.1 | `timerPeriodMs_` メンバ追加（int） | `AudioEngine.h` | ✅ | |
| 4.2 | `timerPeriodMs_ = 100` 初期化 | `Init.cpp` | ✅ | |
| 4.3 | timerCallback先頭: interval計測（相対閾値） | `Timer.cpp` | ✅ | max(10ms, expected×10%) |
| 4.4 | timerCallback末尾: exec計測（10ms以上のみ出力） | `Timer.cpp` | ✅ | |

## Step 5: フェーズ別メモリ ✅

| # | タスク | ファイル | 状態 | 備考 |
|---|-------|---------|------|------|
| 5.1 | build後: memAfterBuild = getProcessMemoryInfo() | `RebuildDispatch.cpp` | ✅ | |
| 5.2 | rebuildIR後: memAfterIR = getProcessMemoryInfo() | `RebuildDispatch.cpp` | ✅ | BUILD_PHASEに統合 |
| 5.3 | commit前: memBeforeCommit = getProcessMemoryInfo() | `RebuildDispatch.cpp` | ✅ | BUILD_PHASEに統合 |
| 5.4 | ログ出力（Private/WS/PF + Delta） | `RebuildDispatch.cpp` | ✅ | |

## Step 6: クロスフェード開始＋終了 ✅

| # | タスク | ファイル | 状態 | 備考 |
|---|-------|---------|------|------|
| 6.1 | XFADE start検出（isPending遷移） | `Timer.cpp` | ✅ | |
| 6.2 | XFADE completedログ（既存fade完了ブロックに追記） | `Timer.cpp` | ✅ | |

## コンパイル確認 ✅

| # | ファイル | 状態 | 備考 |
|---|---------|------|------|
| C.1 | `DiagnosticsConfig.h` | ✅ | コンパイル成功 |
| C.2 | `AudioEngine.h` | ✅ | コンパイル成功 |
| C.3 | `AudioEngine.Timer.cpp` | ✅ | コンパイル成功（3 warnings） |
| C.4 | `AudioEngine.RebuildDispatch.cpp` | ✅ | コンパイル成功 |
| C.5 | `AudioEngine.Commit.cpp` | ✅ | コンパイル成功 |
| C.6 | `AudioEngine.Init.cpp` | ✅ | コンパイル成功 |
| C.7 | リンク | ✅ | MSVCビルド成功。ICXはlibircmt.lib設定修正済み |

## 未実装（今後の拡張）

| # | 項目 | 理由 |
|---|------|------|
| U.1 | AudioBlock.cppのupdateAtomicMaximum()呼び出し | Audio Threadのcallback末尾でcbMaxUs/intervalMaxUs/callbackCountを更新する実装。AudioBlock.cppの詳細構造確認が必要なため別PRとする |
| U.2 | XFADE cancelled（中断） | 枠のみ定義。現時点では実装不要 |
| U.3 | XFADE generation（old→new） | currentGen/targetGenの取得方法確認中 |
| U.4 | Obsolete buildElapsed/rebuildIRElapsed | plan設計だが未実装 |
