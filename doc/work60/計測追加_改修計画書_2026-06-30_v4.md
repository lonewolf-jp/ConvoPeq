# 計測追加 改修計画書 v4（レビュー反映版）

**日付**: 2026-06-30 | **対象**: ConvoPeq 3rd-observation | **総合評価（User）**: 9.4-9.5/10

---

## 変更一覧

| ID | 計測内容 | 優先度 | 影響ファイル | 難易度 | 推定工数 |
| --- | --- | --- | --- | --- | --- |
| A | getNextAudioBlock() 受信タイムスタンプ | ★★★★★ | AudioBlock.cpp, BlockDouble.cpp, AudioEngine.h | 低 | 30分 |
| F | diagLog Release対応（基盤） | ★★★★★ | diagLog定義ファイル（9箇所） | 低 | 10分 |
| C | ConvolverProcessor::process() 実行時間 | ★★★★★ | ConvolverProcessor.Runtime.cpp | 低 | 20分 |
| C2 | StereoConvolver::process() 単体時間 | ★★★★★ | ConvolverProcessor.Runtime.cpp | 低 | 15分 |
| G | CPU migration 記録（+ callbackIndex/gen/seq） | ★★★★★ | AudioBlock.cpp, BlockDouble.cpp, AudioEngine.h | 低 | 15分 |
| H | callback開始時 publicationSequence | ★★★★★ | AudioBlock.cpp, BlockDouble.cpp | 低 | 10分 |
| D | EQ::process() 実行時間（+ bands/order） | ★★★★☆ | DSPCoreDouble.cpp, DSPCoreFloat.cpp | 低 | 30分 |
| B | Callback処理時間 リングバッファ（+ commit seq） | ★★★☆☆ | AudioBlock.cpp, AudioEngine.h | 低 | 25分 |
| E | ANS applyMatchedCoefficients | ★★★☆☆ | DSPCoreDouble.cpp, DSPCoreIO.cpp | 低 | 15分 |

---

## 設計原則（全改修共通）

### 共通キー
全ログ出力に以下の共通キーを付与し、複数計測の相関解析を可能にする:
- **callbackIndex**: 現在の audioCallbackEpochCounter 値
- **gen**: runtimeWorld->generation
- **seq**: runtimeWorld->metadata.publicationSequence

### budget% 付与
全時間計測に expectedIntervalUs と budget%（使用率）を付与。
絶対時間（us）と相対時間（%）の両方で解析可能。

### memory_order
診断専用値は全て memory_order_relaxed。他スレッドとの同期対象外。

---

## F: diagLog Release対応（基盤整備）

v2/v3から変更なし。Timer.cpp準拠に全9ファイルを統一。

---

## A: コールバック受信タイムスタンプ（最重要）

v3から変更なし。OS/Driver区間ジッター測定。atomic<int64_t>、relaxed。

---

## G: CPU migration 記録（改善版）

### v3からの改善点
- exchangeをやめ、load + 比較 + store に変更
- 出力に callbackIndex, seq, gen を追加（相関解析用）

### コード（AudioBlock.cpp / BlockDouble.cpp）
```
#if CONVOPEQ_ENABLE_RUNTIME_DIAGNOSTICS
{
    const uint32_t cpu = static_cast<uint32_t>(::GetCurrentProcessorNumber());
    const uint32_t prev = rtLocalState_.lastCallbackProcessor.load(
        std::memory_order_relaxed);
    if (prev != cpu) {
        rtLocalState_.lastCallbackProcessor.store(cpu, std::memory_order_relaxed);
        if (prev != UINT32_MAX) {
            convo::fetchAddAtomic(rtLocalState_.cpuMigrationCount,
                uint64_t{1}, std::memory_order_relaxed);
            const uint64_t cbIdx = convo::consumeAtomic(
                rtLocalState_.audioCallbackEpochCounter, std::memory_order_relaxed);
            const uint64_t pubSeq = (runtimeWorld != nullptr)
                ? runtimeWorld->metadata.publicationSequence : 0;
            const uint64_t gen = (runtimeWorld != nullptr)
                ? static_cast<uint64_t>(runtimeWorld->generation) : 0;
            juce::Logger::writeToLog(
                "[CPU_MIG] callback=" + juce::String((int64_t)cbIdx)
                + " seq=" + juce::String((int64_t)pubSeq)
                + " gen=" + juce::String((int64_t)gen)
                + " cpu=" + juce::String((int)cpu)
                + " prev=" + juce::String((int)prev));
        }
    }
}
#endif
```

---

## H: callback開始時 publicationSequence（exchange除去版）

### v3からの改善点
- exchange を load + 比較 + store に変更（LOCK命令回避）
- callbackIndex 付与

### コード（AudioBlock.cpp / BlockDouble.cpp）
```
#if CONVOPEQ_ENABLE_RUNTIME_DIAGNOSTICS
{
    if (runtimeWorld != nullptr) {
        const uint64_t seq = runtimeWorld->metadata.publicationSequence;
        const uint64_t prevSeq = rtLocalState_.lastCallbackPublicationSeq.load(
            std::memory_order_relaxed);
        if (seq != prevSeq) {
            rtLocalState_.lastCallbackPublicationSeq.store(
                seq, std::memory_order_relaxed);
            if (prevSeq > 0) {
                juce::Logger::writeToLog(
                    "[CB_SEQ] callback=" + juce::String((int64_t)thisCallbackIndex)
                    + " gen=" + juce::String((int64_t)runtimeWorld->generation)
                    + " seq=" + juce::String((int64_t)seq)
                    + " prevSeq=" + juce::String((int64_t)prevSeq));
            }
        }
    }
}
#endif
```

---

## C: ConvolverProcessor::process() 実行時間（+ FFT info）

### v3からの改善点
- 出力に callQuantum（NUC分割数）、latency（FFT関連）を追加
- 閾値: max(300us, expectedIntervalUs * 0.2)
- budget%、共通キー（callbackIndex, gen, seq）を付与

### コード（ConvolverProcessor.Runtime.cpp）
```
// ログ出力部
juce::String log("[CONV_TIME]");
log += " cb=" + juce::String((int64_t)callbackIdx);
log += " gen=" + juce::String((int64_t)generation);
log += " seq=" + juce::String((int64_t)pubSeq);
log += " us=" + juce::String((int64_t)convElapsedUs);
log += " budget=" + juce::String(budgetPct, 1) + "%";
if (conv != nullptr) {
    log += " callQ=" + juce::String(conv->callQuantumSamples);
    log += " lat=" + juce::String(conv->latency);
    log += " block=" + juce::String(block.getNumSamples());
}
```

---

## C2: StereoConvolver::process() 単体時間

v3から変更なし。同様に共通キー、budget%を付与。
閾値: max(200us, expected * 0.15)。

---

## D: EQ::process() 実行時間（+ bands/order）

### v3からの改善点
- 出力に order（Conv->EQ / EQ->Conv）を追加
- bands数、budget%、共通キーを付与

### コード（DSPCoreDouble.cpp / DSPCoreFloat.cpp）
```
int activeBands = 0;
if (eqParamsToUse != nullptr) {
    for (int i = 0; i < 20; ++i)
        if (eqParamsToUse->bands[i].enabled
            && fabs(eqParamsToUse->bands[i].gain) > 0.01f)
            ++activeBands;
}
const char* orderStr = (state.order == ProcessingOrder::ConvolverThenEQ)
    ? "Conv->EQ" : "EQ->Conv";
juce::String log("[EQ_TIME]");
log += " cb=" + juce::String((int64_t)thisCallbackIndex);
log += " us=" + juce::String((int64_t)eqElapsedUs);
log += " bands=" + juce::String(activeBands);
log += " order=" + juce::String(orderStr);
log += " budget=" + juce::String(budgetPct, 1) + "%";
diagLog(log);
```

---

## B: Callback処理時間 リングバッファ（commit seq付き）

### v3からの改善点
途中状態読取防止のため、コミットシーケンス（sequence）パターンを導入。
書き込み側: データ書込→最後に atomic_store(sequence)
読み取り側: sequence読取→全データ読取→sequence再確認（一致すれば整合性保証）

### 構造体定義（AudioEngine.h / RTLocalState）
```cpp
static constexpr size_t kCallbackTimingSlots = 16;
struct CallbackTimingEntry {
    uint64_t callbackIndex = 0;
    uint64_t processTimeUs = 0;
    int64_t driftUs = 0;
    uint32_t cpu = UINT32_MAX;
    double budgetPct = 0.0;
    std::atomic<uint64_t> sequence{0};
};
CallbackTimingEntry callbackTimingHistory[kCallbackTimingSlots];
std::atomic<uint64_t> callbackTimingWriteCount{0};
```

### 書き込み側（callback末尾）
```cpp
const uint64_t wc = convo::fetchAddAtomic(
    rtLocalState_.callbackTimingWriteCount, uint64_t{1}, std::memory_order_relaxed);
const size_t idx = static_cast<size_t>((wc - 1) % kCallbackTimingSlots);
auto& entry = rtLocalState_.callbackTimingHistory[idx];
// 1) 非atomicフィールドを先に書く
entry.callbackIndex = thisCallbackIndex;
entry.processTimeUs = processTime;
entry.driftUs = rtLocalState_.lastCallbackDriftUs.load(std::memory_order_relaxed);
entry.cpu = rtLocalState_.lastCallbackProcessor.load(std::memory_order_relaxed);
entry.budgetPct = budgetPct;
// 2) 最後に sequence を更新（commit: releaseで書込み完了を保証）
entry.sequence.store(wc, std::memory_order_release);
```

### 読み取り側（XRUN時）
```cpp
if (xrunDetected) {
    const uint64_t wc = convo::consumeAtomic(
        rtLocalState_.callbackTimingWriteCount, std::memory_order_relaxed);
    for (uint64_t i = wc; i > wc - kCallbackTimingSlots && i > 0; --i) {
        const size_t idx = (i - 1) % kCallbackTimingSlots;
        const auto& e = rtLocalState_.callbackTimingHistory[idx];
        const uint64_t seq = e.sequence.load(std::memory_order_acquire);
        if (seq == 0) continue;
        // データ一貫性確認
        const uint64_t cbIdx = e.callbackIndex;
        const uint64_t proc = e.processTimeUs;
        const int64_t drift = e.driftUs;
        const uint32_t cpu = e.cpu;
        const double pct = e.budgetPct;
        if (e.sequence.load(std::memory_order_acquire) == seq) {
            juce::Logger::writeToLog(
                "[CB_HIST] idx=" + juce::String((int64_t)cbIdx)
                + " proc=" + juce::String((int64_t)proc)
                + " drift=" + juce::String((int64_t)drift)
                + " cpu=" + juce::String((int)cpu)
                + " budget=" + juce::String(pct, 1) + "%");
        }
    }
}
```

---

## E: ANS applyMatchedCoefficients 実行時間

優先度低。Step 8実装。v3から変更なし。

---

## 実装順序

| Step | ID | 内容 | 理由 |
| --- | --- | --- | --- |
| 1 | F | diagLog統一 | 基盤整備（全診断ログのRelease動作保証） |
| 2 | A | callback entry | OS/Driver区間可視化（最重要） |
| 3 | G | CPU migration | OS問題検出（+共通キー） |
| 4 | H | publicationSequence | 伝搬経路追跡（exchange除去版） |
| 5 | C | Convolver全体 | MKL FFT時間（+FFT info） |
| 6 | C2 | StereoConv個別 | FFT vs RCU管理の分離 |
| 7 | D | EQ timing+bands | EQ時間（+order） |
| 8 | B | CB hist ringbuf | XRUN時履歴（commit seq付き） |
| 9 | E | ANS timing | 確認用 |

---

## 仮説判定マトリクス（v4版）

| 観測パターン | 示唆する原因 |
| --- | --- |
| DriftUs大 + CPU_MIG有 | OSスケジューラ問題（CPU migration + 遅延） |
| DriftUs大 + CPU_MIG無 | OS/Driver DPC/ISR問題 |
| DriftUs小 + CB_SEQ変化無 + CONV_TIME大 + STCONV_TIME大 | FFT畳み込み遅延 |
| DriftUs小 + CB_SEQ変化無 + CONV_TIME大 + STCONV_TIME小 | Convolver管理処理遅延（RCU等） |
| DriftUs小 + EQ_TIME大 | EQ IIRフィルタ遅延 |
| DriftUs小 + CB_HIST漸増傾向 | メモリプレッシャーまたは経年劣化 |
| DriftUs小 + CB_HIST急増 + CPU_MIG有 | OS要因（コンテキストスイッチ） |
| 全計測値小 | 偶発的OSジッター |

---

## RT負荷評価

| ID | 追加処理 | 推定コスト |
| --- | --- | --- |
| F | なし（コンパイル時制御） | 0 |
| A | load + store + getCurrentTimeUs | 数十〜数百ns程度 |
| G | GetCurrentProcessorNumber() + load/store | 数十ns程度 |
| H | relaxed load + 比較 + store | 数十ns程度 |
| C | getCurrentTimeUs x2 + ログ（閾値超過時のみ） | 数十〜数百ns程度 |
| C2 | getCurrentTimeUs x2ch + ログ（閾値超過時のみ） | 数十〜数百ns程度 |
| D | getCurrentTimeUs x2 + bandカウント（閾値超過時のみ） | 数十〜数百ns程度 |
| B | リングバッファ書込（常時）+ sequence store(release) | 数十ns（常時） |
| E | getCurrentTimeUs x2（係数切替時のみ） | 数十〜数百ns程度 |

全計測最大負荷: 約1us未満（全閾値超過時）。通常時: 数百ns程度。

---

## v3->v4差分サマリ

| 項目 | v3 | v4 |
| --- | --- | --- |
| G: exchange除去 | exchangeAtomic | load + 比較 + store |
| G: 出力拡張 | CPU番号のみ | +callbackIndex, gen, seq |
| H: exchange除去 | exchangeAtomic | load + 比較 + store |
| B: 整合性保証 | 途中状態リスク有 | commit seqパターンで完全防御 |
| C: FFT info | blockSizeのみ | +callQuantum, latency |
| D: order | bandsのみ | +processingOrder（Conv->EQ / EQ->Conv） |
| 全ログ: budget% | なし | 全計測にbudget%追加 |
| 全ログ: 共通キー | 別々の形式 | callbackIndex, gen, seq統一 |

---

*本計画書はUserレビュー（v3->v4）を反映。v1からの累積改善項目: atomic<int64_t>, relaxed統一, load+store, 条件付き計測, ハイブリッド閾値, StereoConvolver分離, CPU migration, publicationSequence, リングバッファcommit seq, EQ bands/order, budget%, 共通キー統一, E優先度降格。*
