# R11〜R25 Closed最短修正順（失敗件数削減インパクト順）実装タスク

作成日: 2026-05-21
対象: `doc/work/plan5.md` / `ISR_Completeness_Risk_Backlog.md` の R11〜R25

---

## 目的

本書は、R11〜R25 を **Closed** に到達させるための実装タスクを、
「理想的な設計順」ではなく **CI失敗件数を最短で減らす順** に並べた実行計画として定義する。

優先指標:

1. CI Failures の即時削減
2. strict scan の通過
3. Closed最小検証項目の充足

---

## 現状（2026-05-21 計測）

- `Strict Atomic Dot-Call Scan`: 失敗（73 violations）
- `check-list-compliance.ps1`: 失敗（Failures 45 / Warnings 9）
- `Release Build`: 成功

含意:

- ビルド成功は維持しているが、R18の merge blocker 前提には未到達。
- `TODO/stub` と `atomic dot-call` が fail 主因。

---

## 実行フェーズ（最短順）

## Phase 0: 失敗件数の即時圧縮（最優先）

対象R: R18, R25, R11, R12, R14, R15, R16, R20, R24

### Task 0-1: ISR系 TODO コメント除去と状態明示

対象ファイル:

- `src/audioengine/ISRClosureGraphWalker.cpp`
- `src/audioengine/ISRBarrierOptimizer.cpp`
- `src/audioengine/ISRClosure.cpp`
- `src/audioengine/ISRDebugRuntime.cpp`
- `src/audioengine/ISRHB.cpp`
- `src/audioengine/ISRRetireRuntimeEx.cpp`
- `src/audioengine/ISRRTExecution.cpp`
- `src/audioengine/ISRRuntimePublicationCoordinator.cpp`

作業:

- lint fail を誘発する危険コメント語（TODO/FIXME/workaround/temporary等）を除去。
- スタブ箇所は build profile 方針（Release minimal / Debug partial / CI full）に合わせて動作モードを明記。

完了条件（DoD）:

- `check-list-compliance.ps1` の 15.1 起因 failures が 0。

---

### Task 0-2: atomic dot-call の helper 化

対象ファイル:

- `src/audioengine/ISRSealedObject.h`
- `src/audioengine/ISRDSPHandle.cpp`
- `src/audioengine/ISRDSPQuarantine.cpp`
- `src/audioengine/ISRHB.cpp`
- `src/audioengine/ISRLifecycle.cpp`
- `src/audioengine/ISRRetire.cpp`
- `src/audioengine/ISRRTExecution.cpp`
- `src/audioengine/ISRRuntimePublicationCoordinator.cpp`
- `src/audioengine/ISRShutdown.cpp`

作業:

- `.load/.store/.exchange/.fetch_add/...` を `convo::consumeAtomic/publishAtomic/exchangeAtomic/fetchAddAtomic` 系へ置換。
- `memory_order_seq_cst` を排除し、既存規約に従った order を適用。

完了条件（DoD）:

- `Strict Atomic Dot-Call Scan` が Pass。
- `check-list-compliance.ps1` の 3.1 起因 failure が 0。

---

## Phase 1: R11/R12/R14 の実働化

対象R: R11, R12, R14, R21, R22

### Task 1-1: Closure validation 実用化（R11）

対象:

- `src/audioengine/ISRClosure.h`
- `src/audioengine/ISRClosure.cpp`
- `src/audioengine/ISRClosureGraphWalker.h`
- `src/audioengine/ISRClosureGraphWalker.cpp`

作業:

- node 重複 ID 検出
- edge 参照先存在検証
- cycle 検出（DFS）
- closure artifact 出力（最低限 schema 準拠）

DoD:

- `validateClosureGraph` の fail ケース（最低3類型）を再現テストで確認。
- closure 不整合時に publish 拒否。

---

### Task 1-2: Payload tier enforce 実装（R12）

対象:

- `src/audioengine/ISRPayloadTier.h`
- `src/audioengine/ISRPayloadTier.cpp`
- publish 経路（RuntimePublication の受理前検証）

作業:

- Forbidden / RTLocalOnly の混入拒否
- tier 依存順序違反検出

DoD:

- Forbidden 混入時に検出 fail。
- RTLocalOnly 混入時に検出 fail。

---

### Task 1-3: Retire intent bridge 完結（R14）

対象:

- `src/audioengine/ISRRetire.h`
- `src/audioengine/ISRRetire.cpp`
- `src/audioengine/ISRRetireRuntimeEx.h`
- `src/audioengine/ISRRetireRuntimeEx.cpp`

作業:

- RT は intent emit のみ許可
- NonRT coordinator で enqueue/reclaim を担当
- `acknowledgeRetireCoordination` を実装

DoD:

- RT 直enqueue を禁止（検出テストあり）。
- RT→NonRT→authority enqueue 経路の再現テスト pass。

---

## Phase 2: Shutdown/HB の証明可能化

対象R: R15, R16, R24

### Task 2-1: Shutdown FSM を正本準拠化（R15/R24）

対象:

- `src/audioengine/ISRShutdown.h`
- `src/audioengine/ISRShutdown.cpp`

作業:

- phase enum を正本記述へ合わせる（逆行/飛び越し拒否）
- shutdown trace 出力を実装
- SH-1〜SH-4 検証フックを実装

DoD:

- phase 逆行/飛び越しを fail 検出。
- late callback / post-stop enqueue 検出テスト pass。

---

### Task 2-2: HB trace/verifier 実装（R16）

対象:

- `src/audioengine/ISRHB.h`
- `src/audioengine/ISRHB.cpp`

作業:

- HB trace JSON 出力
- required edge 検証
- reorder simulation（forced reorder / epoch lag / retire delay / observe race）最小セット

DoD:

- required HB 欠落で fail。
- required HB 充足で pass。

---

## Phase 3: REV3.2 運用固定のコード化

対象R: R19, R20, R23, R25

### Task 3-1: capability-first authority 導入（R19）

対象:

- Runtime core API 層（publish/retire/shutdown）

作業:

- `PublishAuthority` / `RetireAuthority` / `ShutdownAuthority` tag 導入
- 危険 API へ capability 要求を追加

DoD:

- capability 無し経路がコンパイル不能または fail-fast。

---

### Task 3-2: Host chaos normalization 実装（R20）

対象:

- lifecycle adapter / normalization 層

作業:

- duplicate prepare collapse
- release-before-prepare reject
- callback during Releasing reject

DoD:

- HC-1/HC-2/HC-3 の再現テスト pass。

---

### Task 3-3: 2-world 固定型の明示（R23）

対象:

- runtime boundary 定義と参照経路

作業:

- `RuntimeBoundary` と `PublicationWorld / ExecutionWorld` を明示
- federation 常駐導入を抑制

DoD:

- world 境界が型レベルで追跡可能。

---

### Task 3-4: DebugRuntime CI限定化の固定（R25）

対象:

- `src/audioengine/ISRDebugRuntime.*`
- build profile 分岐（Release/Debug/CI）

作業:

- Release: proof/trace最小
- Debug: partial
- CI: full verify

DoD:

- build profile ごとの機能差が自動検証される。

---

## Closed判定ゲート（R11〜R25）

以下を満たした時点で、対象Rを Closed 判定対象に上げる。

1. `Strict Atomic Dot-Call Scan` Pass
2. `check-list-compliance.ps1` Failures = 0
3. Rごとの Closed最小検証項目（`ISR_Completeness_Risk_Backlog.md`）を証跡付きで満了
4. Artifact policy が build profile と整合
   - Release: minimal
   - Debug: partial
   - CI: full
5. 証跡（テスト名/ログ/artifact path）を本書または関連検証ログへリンク

---

## 実行順サマリ（短縮版）

1. TODO/危険コメント起因 fail を除去
2. atomic dot-call 73件を helper 化
3. Closure / Payload / Retire bridge を実働化
4. Shutdown / HB を証明可能化
5. capability-first / 2-world / Debug層分離を固定

---

## 変更管理メモ

- 本書は「最短修正順の運用計画」であり、仕様正本の置換ではない。
- R採番や判定基準の正本は `ISR_Completeness_Risk_Backlog.md` を参照すること。
- 解釈衝突時は `plan5.md` REV3.2 方針を優先すること。
