# R11〜R25 Closed最短実装順（Step 0〜1差分設計）

作成日: 2026-05-21
対象: `doc/work/plan5.md` / `doc/work/ISR_Completeness_Risk_Backlog.md`

---

## 1. ここまでの調査結論（要約）

- ISR関連クラスは `AudioEngine` に多数メンバとして存在するが、実行経路で未接続のものが多い。
- `LifecycleIsolationRuntime` は `prepareToPlay/getNextAudioBlock/releaseResources` に接続済み。
- 証跡出力 (`ISREvidenceExporter`) はプレースホルダ中心で、呼び出し経路が弱い。
- CI検証は実質 V1 中心（Atomic dot-call）で、R18 の V1〜V10 merge blocker には未到達。
- 実測:
  - Strict Atomic Dot-Call Scan: **Pass**
  - `check-list-compliance.ps1`: **Failures 0 / Warnings 9**
  - `src/audioengine` のエラー: **No errors**

---

## 2. 最短クリティカルパス（再掲）

1. **Step 0: 結線**（未使用ISR部品を publish/retire/evidence 経路へ接続）
2. **Step 1: R11/R12 実働化**（Closure + Payload Tier の publish 前 reject）
3. Step 2以降（R13/R14/R15/R16/R18...）

本書では **Step 0〜1** の実装差分を関数単位で固定する。

---

## 3. Step 0 実装差分（結線）

### 0-1. 共通フック追加

- **ファイル**: `src/audioengine/AudioEngine.h`
- **追加位置**: `private:`（`runtimeStore`/`RuntimePublicationBridge` 近傍）
- **追加行数目安**: +22〜28
- **追加シグネチャ**:
  - `bool runPublicationPrecheckNonRt(const RuntimePublishWorld& world) noexcept;`
  - `void onRuntimePublishedNonRt(const RuntimePublishWorld& world) noexcept;`
  - `void onRuntimeRetiredNonRt(const RuntimePublishWorld* world) noexcept;`
  - `void emitEvidenceTickNonRt(bool force) noexcept;`

### 0-2. Bridgeに pre/post hook 追加

- **ファイル**: `src/audioengine/AudioEngine.h`
- **対象**: `class RuntimePublicationBridge`
- **追加行数目安**: +26〜34
- **追加関数**:
  - `validatePublicationNonRt(...)`
  - `didPublishRuntimeNonRt(...)`
  - `willRetireRuntimeNonRt(...)`

### 0-3. core publish 経路で hook 呼び出し

- **ファイル**: `src/core/RuntimePublicationCoordinator.h`
- **対象関数**: `publishState(...)`
- **追加行数目安**: +9〜13
- **変更内容**:
  - publish前に bridge precheck 実行
  - precheck fail時は publishせず retireへ
  - publish後に `didPublishRuntimeNonRt`
  - old retire前に `willRetireRuntimeNonRt`

### 0-4. commit成功後に evidence tick

- **ファイル**: `src/audioengine/AudioEngine.Commit.cpp`
- **対象関数**: `commitNewDSP(...)`
- **追加行数目安**: +6〜10
- **変更内容**:
  - publish確定後に `emitEvidenceTickNonRt(false)`
  - Debug/CIで `debugRuntime_.emitCIArtifacts()` を発火

### 0-5. timerで周期evidence / shutdownで強制flush

- **ファイル**: `src/audioengine/AudioEngine.Timer.cpp`
- **対象関数**: `timerCallback()`
- **追加行数目安**: +10〜16
- **変更内容**:
  - 周期 `emitEvidenceTickNonRt(false)`
  - shutdown中 `emitEvidenceTickNonRt(true)`

### 0-6. release終端で最終flush

- **ファイル**: `src/audioengine/AudioEngine.Processing.ReleaseResources.cpp`
- **対象関数**: `releaseResources()`
- **追加行数目安**: +4〜7
- **変更内容**:
  - `clearPublishedRuntimeSnapshotsNonRt()` 後に `emitEvidenceTickNonRt(true)`

---

## 4. Step 1 実装差分（R11/R12）

### 1-1. Closure validation と artifact の連動

- **ファイル**: `src/audioengine/ISRClosureGraphWalker.cpp`
- **対象関数**: `validateGraph(...)`
- **追加行数目安**: +8〜12
- **内容**:
  - validation結果を取得
  - success/fail いずれも artifact 出力可能に統一

### 1-2. Closure artifact 必須項目の追加

- **ファイル**: `src/audioengine/ISRClosureGraphWalker.cpp`
- **対象関数**: `emitClosureArtifact(...)`
- **追加行数目安**: +12〜18
- **項目追加**:
  - `status`, `validationErrors`, `timestamp`, `buildMode`

### 1-3. Payload Tier reject 理由の明示

- **ファイル**: `src/audioengine/ISRPayloadTier.h`
- **追加行数目安**: +8〜12
- **追加**:
  - `enum class TierRejectReason`
  - `TierRejectReason explainPublishReject(...) const noexcept;`

- **ファイル**: `src/audioengine/ISRPayloadTier.cpp`
- **対象関数**: `isPublishAllowed(...)`
- **追加行数目安**: +10〜16
- **内容**:
  - `explainPublishReject` を用いて判定を一元化

### 1-4. ISR publication precheck API 追加

- **ファイル**: `src/audioengine/ISRRuntimePublicationCoordinator.h`
- **追加行数目安**: +10〜16
- **追加**:
  - `bool precheckPublish(const PayloadClosureDescriptor&, const TieredPayloadDescriptor&) noexcept;`
  - `const char* lastRejectReason() const noexcept;`（任意）

- **ファイル**: `src/audioengine/ISRRuntimePublicationCoordinator.cpp`
- **追加行数目安**: +24〜36
- **内容**:
  - Closure + Tier の合成判定
  - fail時 `false` を返す

### 1-5. AudioEngine側 precheck 実装

- **ファイル**: `src/audioengine/AudioEngine.Commit.cpp`
- **新規関数**: `runPublicationPrecheckNonRt(...)`
- **追加行数目安**: +35〜55
- **内容**:
  - `RuntimePublishWorld` から最小descriptor生成
  - `runtimePublicationCoordinator_.precheckPublish(...)`
  - fail時に debug/evidence を発火し reject

### 1-6. publish/retire posthook 実装

- **ファイル**: `src/audioengine/AudioEngine.Commit.cpp`
- **新規関数**:
  - `onRuntimePublishedNonRt(...)`
  - `onRuntimeRetiredNonRt(...)`
  - `emitEvidenceTickNonRt(...)`
- **追加行数目安**: +40〜65
- **内容**:
  - capability-first commit/retire 呼び出し
  - `retireRuntimeEx_` 連携
  - rate-limited evidence export

---

## 5. 適用順（推奨コミット順）

1. `AudioEngine.h`（宣言 + Bridge hook）
2. `src/core/RuntimePublicationCoordinator.h`（publish hook）
3. `ISRPayloadTier.h/.cpp`（reject reason）
4. `ISRRuntimePublicationCoordinator.h/.cpp`（precheck API）
5. `ISRClosureGraphWalker.cpp`（artifact拡張）
6. `AudioEngine.Commit.cpp`（本体接続）
7. `AudioEngine.Timer.cpp`（周期/強制 evidence）
8. `AudioEngine.Processing.ReleaseResources.cpp`（終端flush）

---

## 6. 各コミットの検証ゲート

- **必須**（毎コミット）
  - Strict Atomic Dot-Call Scan
  - `check-list-compliance.ps1`
- **推奨**（3〜4ファイル変更ごと）
  - Release build
- **Step 1完了時**
  - `evidence/*.json` 生成確認（最低: closure/mutation/hb/retire/shutdown + budget/recovery/snapshot）

---

## 7. Closed判定への接続（Step 0〜1範囲）

- R11: closure precheck mandatory + artifact
- R12: forbidden/RTLocal reject mandatory + reason trace
- R18（前進）: evidence producer 経路を runtime 実行に接続

> 注: R18 の最終 Closed には V1〜V10 CI merge blocker 実装が必要（本書は前段の実装差分定義）。
