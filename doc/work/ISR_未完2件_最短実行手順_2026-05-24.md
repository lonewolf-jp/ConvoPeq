# ISR 未完2件の最短実行手順（1ページ）

対象: `doc/work/ISR_バグ精査結果_実装修正タスク分解_2026-05-23.md` の未チェック2項目のみ

- [ ] 世代混在検出テスト結果（hash一致）
- [ ] A/B音声比較結果（OS on/off, snapshot/bypass遷移）

---

## 0. ゴール（完了条件）

以下を満たしたら完了:

1. **hash一致結果**を1行で記録（不一致0件）
2. **A/B比較結果**を1行で記録（回帰なし）
3. 上記2行を `ISR_バグ精査結果_実装修正タスク分解_2026-05-23.md` に追記し、該当チェックを `[x]` 化

---

## 1. 事前固定（5分）

- ビルドは既に通過済み状態を前提
- 比較素材は既存の `sampledata/test_music.wav` を使用
- 実行条件は統一（同一SR/Buffer/入力）

---

## 2. 未完① 世代混在検出（hash一致）

### 実行（世代混在検出）

1. 現在の検出ロジックで、1ブロック内の snapshot hash と EQ参照 hash を採取
2. 高頻度パラメータ更新をかけた状態で短時間ラン
3. 不一致件数を集計

### 記録フォーマット（世代混在検出）

- `世代混在検出: mismatch=0（hash一致） / 条件: <SR, Buffer, 実行時間, 入力素材>`

### 判定（世代混在検出）

- `mismatch=0` なら Pass
- 1件以上なら Fail（完了扱いにしない）

---

## 3. 未完② A/B音声比較（OS on/off, snapshot/bypass遷移）

### 実行（A/B比較）

1. **A（baseline）** と **B（candidate）** を同条件で出力
2. 以下3観点で比較（聴感 + ログ）
   - OS on/off
   - snapshot遷移
   - bypass遷移
3. クリック/段差/明確な回帰の有無を判定

### 記録フォーマット（A/B比較）

- `A/B音声比較: 回帰なし（OS on/off, snapshot遷移, bypass遷移） / 条件: <SR, Buffer, 入力素材>`

### 判定（A/B比較）

- 回帰なしなら Pass
- 回帰ありなら Fail（完了扱いにしない）

---

## 4. ドキュメント反映（3分）

反映先:

- `doc/work/ISR_バグ精査結果_実装修正タスク分解_2026-05-23.md`

更新内容:

1. 「最新実行証跡」または「E. 検証証跡」に、上記2行の結果を追記
2. チェックリストを更新
   - `[x] 世代混在検出テスト結果（hash一致）`
   - `[x] A/B音声比較結果（OS on/off, snapshot/bypass遷移）`

---

## 5. 最短版チェックリスト（実行者用）

- [ ] hash採取ラン実施
- [ ] mismatch=0確認
- [ ] A/B比較実施（3観点）
- [ ] 回帰なし確認
- [ ] 実装修正文書へ2行追記
- [ ] 未完2件を `[x]` 化

以上。
