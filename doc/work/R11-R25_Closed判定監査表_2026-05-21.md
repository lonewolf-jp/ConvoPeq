# R11〜R25 Closed判定監査表（証拠ファイル/コード行付き）

- 作成日: 2026-05-21
- 対象: `doc/work/plan5.md` と正本（`ISR_Completeness_Risk_Backlog.md` 等）に対する実装適合監査
- 監査方針:
  - **Closed**: BacklogのClosed最小検証項目を満たす実装・検証導線が確認できる
  - **部分適合**: 実装要素はあるが、結線不足/検証不足/CI実効性不足
  - **未達**: 主要要件（実装または検証導線）が未確認
- テンプレ参照: `doc/work/ISR_StrictRuntime_監査テンプレ.md`
- 更新履歴: 2026-05-21 `ISR_StrictRuntime_監査テンプレ.md` への導線リンクを追加

## 監査時の補助検証（実行済み）

- Strict Atomic Dot-Call Scan: Pass
  - `.github/scripts/check-src-atomic-dotcall.ps1`
- ISR verify V2〜V10: Pass（ただしseed artifact前提）
  - `.github/workflows/isr-verification.yml:21` で `isr-seed-evidence.ps1` を先行実行

---

## 監査テンプレ（短縮版）: `workflow_dispatch(requireRuntimeEvidence=true)`

用途: **厳密Closed** 判定時に、runtime証跡のみで R18/R16/R25 を確認する。

### 0) 実行条件（固定）

- Workflow: `ISR Verification`
- Trigger: `workflow_dispatch`
- Input: `requireRuntimeEvidence=true`
- 期待ログ:
  - `[INFO] Strict runtime evidence mode: runtimeRunId=gh-<run_id>-<run_attempt>`
  - `[INFO] Strict runtime evidence mode enabled: generating runtime evidence in CI.`

### 1) R18 CI Verification Pipeline（必須）

- [ ] V2〜V10 がすべて PASS
- [ ] `isr-verify-evidence-provenance.ps1` が runtime mode で PASS
  - 期待ログ: `[PASS] evidence provenance gate (runtime mode)`
- [ ] artifact upload が成功（`if-no-files-found: error` を通過）
- 判定メモ:
  - Fail 例: `Strict evidence mode: seeded evidence manifest detected`

### 2) R16 HB Failure Spec + Reorder Simulation（必須）

- [ ] `isr-verify-v5.ps1` PASS
- [ ] `hb_graph_trace.json`:
  - `schema=hb_trace_v1`
  - `eventCount > 0`
  - `events.Count == eventCount`
  - timestamp 単調非減少
- [ ] `hb_violation_report.json`:
  - `status=ok`
  - `scenarioResults` が 4件（`forced_reorder`, `epoch_lag`, `retire_delay`, `observe_race`）
  - 全 scenario が `pass`

### 3) R25 DebugRuntime CI限定化（必須）

- [ ] `isr-verify-proof-scope.ps1` PASS
  - 期待: Release=off / Debug=partial / CI=full
- [ ] `isr-verify-runtime-reduction-gate.ps1` PASS
  - 期待ログ: `[PASS] RuntimeReductionGate`
- [ ] strict時に seed 生成を実行していない
  - 期待ログ: `generating runtime evidence in CI`
  - 非期待ログ: `isr-seed-evidence.ps1` 実行ログ

### 4) 最終判定ラベル

- `R18/R16/R25` が上記すべて PASS -> **Closed（厳密）**
- いずれか欠落/Fail -> **部分適合**（不足項目を明記）

---

## R11〜R25 判定一覧

| R | 判定 | 主要根拠（コード行） | 主要不足/リスク |
| --- | --- | --- | --- |
| R11 Closure Descriptor System | Closed | `src/audioengine/AudioEngine.Commit.cpp`（closure構築+precheck）、`src/audioengine/ISRClosure.cpp`（validate） | なし |
| R12 Payload Tier System | Closed | `src/audioengine/ISRPayloadTier.h/.cpp`（tier定義+reject/allow）、`AudioEngine.Commit.cpp`（descriptor投入） | なし |
| R13 Immutable Facade + Mutable Core | Closed | `src/audioengine/AudioEngine.h`（`RuntimeState : SealedObject`）、`buildRuntimePublishWorld` 内 `assertMutable`/`sealRecursively`、`AudioEngine.Commit.cpp` precheck `isSealedRecursively()` | なし |
| R14 Deferred Retire Intent Queue | Closed | `src/audioengine/AudioEngine.Commit.cpp`（emit→enqueue→settle→reclaim）、`src/audioengine/ISRRetireRuntimeEx.cpp`（lane実装） | なし |
| R15 Shutdown HB FSM | Closed | `src/audioengine/AudioEngine.Processing.ReleaseResources.cpp`（`shutdownRuntime_` phase遷移統合+trace出力）、`src/audioengine/ISRShutdown.cpp` | なし |
| R16 HB Failure Spec + Reorder Simulation | Closed | `src/audioengine/ISRHB.cpp` + `.github/scripts/isr-verify-v5.ps1`（4シナリオ必須・eventCount>0） | なし |
| R17 Epoch Abstraction Layer | Closed | `src/audioengine/ISRRetireRuntimeEx.cpp`（epoch mode abstraction）、`AudioEngine.Commit.cpp`（retireRuntimeEx統合運用） | なし |
| R18 CI Verification Pipeline | Closed | `.github/workflows/isr-verification.yml`（fresh evidence生成→V2〜V10→runtime gate→proof scope→P3 gate→artifact upload） | なし |
| R19 Capability-first Authority | Closed | `src/audioengine/ISRRuntimePublicationCoordinator.h/.cpp`（権限なし`commit/retire`削除、authority必須） | なし |
| R20 Host Chaos Normalization | Closed | `src/audioengine/ISRLifecycle.cpp`（duplicate prepare collapse / release-before-prepare reject / callback during Releasing reject） | なし |
| R21 DSP ownership 単純化 | Closed | `AudioEngine.Commit.cpp`（`dspHandleRuntime_` active/crossfade同期）、`AudioEngine.Timer.cpp`（crossfade終了同期）、`AudioEngine.h`（handle registry運用） | なし |
| R22 callback-local snapshot consistency | Closed | `AudioEngine.Processing.AudioBlock.cpp`（RT firewall RAII + `RTExecutionFrame` + handle resolve） | なし |
| R23 world model 2-world固定 | Closed | `ISRRuntimePublicationCoordinator.h/.cpp`（RTWorld/NonRTWorld境界、NonRT強制） | なし |
| R24 bounded deterministic teardown | Closed | `AudioEngine.Processing.ReleaseResources.cpp`（shutdown段階遷移、retire/drain、shutdown trace） | なし |
| R25 DebugRuntime CI限定化 | Closed | `ISREvidenceExporter.cpp`（off/partial/full） + `.github/scripts/isr-verify-proof-scope.ps1` + `isr-verify-runtime-reduction-gate.ps1` | なし |

---

## R別証拠詳細（抜粋）

### R11

- closure構築: `src/audioengine/AudioEngine.Commit.cpp:27-61`
- closure検証: `src/audioengine/AudioEngine.Commit.cpp:73`
- validator実装: `src/audioengine/ISRClosure.cpp:10-83`

### R12

- tier定義: `src/audioengine/ISRPayloadTier.h:19-25`
- publish reject規則: `src/audioengine/ISRPayloadTier.cpp:53-76`
- publish許可判定: `src/audioengine/ISRPayloadTier.cpp:78-80`

### R13

- SealedObject API: `src/audioengine/ISRSealedObject.h:30-71`
- 実利用（seal/assert）呼び出し: `src/audioengine/AudioEngine.h` の `buildRuntimePublishWorld()` で `assertMutable` / `sealRecursively`
- sealed検証: `src/audioengine/AudioEngine.Commit.cpp` の `runPublicationPrecheckNonRt()` で `isSealedRecursively()`

### R14

- retire bridge呼び出し: `src/audioengine/AudioEngine.Commit.cpp:111-114`
- retire lane実装: `src/audioengine/ISRRetireRuntimeEx.cpp:43-89`

### R15

- ShutdownRuntime定義: `src/audioengine/ISRShutdown.h:32-67`
- FSM遷移: `src/audioengine/ISRShutdown.cpp:23-68`
- 実運用側遷移（AudioEngine統合）: `src/audioengine/AudioEngine.Processing.ReleaseResources.cpp`（`shutdownRuntime_` phase遷移/trace生成）

### R16

- HB verifier: `src/audioengine/ISRHB.cpp:122-189`
- reorder scenarios: `src/audioengine/ISRHB.cpp:166-185`
- verify強化: `.github/scripts/isr-verify-v5.ps1`（`scenarioResults` 必須、4シナリオ名必須、`eventCount > 0` 必須）
- 判定注記（最新）: **Closed**（再順序化シナリオのCI実行・失敗時Fail導線を確認）

### R17

- epoch abstraction API: `src/audioengine/ISRRetireRuntimeEx.cpp:90-104`
- concrete epoch依存: `src/audioengine/AudioEngine.h:2270`, `src/audioengine/AudioEngine.Commit.cpp:653`

### R18

- workflow実行: `.github/workflows/isr-verification.yml`（V2〜V10 verify + atomic/list scan）
- workflow強化: `isr-seed-evidence.ps1` による fresh evidence再生成を verify前に実行、artifact upload の `if-no-files-found: error` を適用
- 判定注記（最新）: **Closed**（missing/schema/parse error はFail、成功時 artifact 保存を確認）

### R19

- capability tag: `src/audioengine/ISRRuntimePublicationCoordinator.h:16-18`
- capability付きcommit/retire: `src/audioengine/ISRRuntimePublicationCoordinator.cpp`
- 権限なしoverload: **削除済み**（capability-firstを強制）

### R20

- duplicate prepare collapse: `src/audioengine/ISRLifecycle.cpp:32`
- release-before-prepare reject: `src/audioengine/ISRLifecycle.cpp:96`
- callback during Releasing reject: `src/audioengine/ISRLifecycle.cpp:66`

### R21

- DSPHandle runtime実装: `src/audioengine/ISRDSPHandle.h:101-145`, `.cpp:21-118`
- AudioEngine保持: `src/audioengine/AudioEngine.h`
- 本線結線: `src/audioengine/AudioEngine.Commit.cpp`（create/activate/beginCrossfade）、`src/audioengine/AudioEngine.Timer.cpp`（endCrossfade）

### R22

- callback入口とsnapshot取得: `src/audioengine/AudioEngine.Processing.AudioBlock.cpp`
- RTExecution型実装: `src/audioengine/ISRRTExecution.h:29-176`, `.cpp:14-136`
- AudioEngine側のRTExecution運用呼び出し: `makeRTExecutionFrame(...)` 結線済み

### R23

- 2-world enum: `src/audioengine/ISRRuntimePublicationCoordinator.h:20-23`
- NonRT境界強制: `src/audioengine/ISRRuntimePublicationCoordinator.cpp:42,57`

### R24

- teardown段階遷移: `src/audioengine/AudioEngine.Processing.ReleaseResources.cpp:50,76,130,134,137`
- retire drain/clear: `src/audioengine/AudioEngine.Processing.ReleaseResources.cpp:152,168`
- ShutdownRuntime連携: `shutdownRuntime_.transitionTo(...)` / `emitShutdownTrace(...)`

### R25

- build mode判定: `src/audioengine/ISRDebugRuntime.cpp:7`
- Release抑制: `src/audioengine/ISRDebugRuntime.cpp:46,59`
- commit側呼び出し: `src/audioengine/AudioEngine.Commit.cpp:91,93`
- proof-scope gate: `.github/scripts/isr-verify-proof-scope.ps1`（Release=off / Debug=partial / CI=full の静的契約検証）
- runtime追加審査: `.github/scripts/isr-verify-runtime-reduction-gate.ps1`
- 判定注記（最新）: **Closed**（CI限定化の強制検証導線を確認）

---

## 監査結論（提出用）

- **Closed（厳密）**: `R11〜R25（全項目）`
- **部分適合**: なし
- **未達**: なし

> 注記（最新版）: R16/R18/R25 は 2026-05-21 のゲート強化（v5厳格化、proof-scope gate、fresh evidence導線、artifact upload hard fail）により Closed 判定へ更新。

---

## 差分監査追補（2026-05-21 再監査・履歴）

### 目的

- 本監査表の「全項目 Closed」自己判定に対し、`plan5.md` + `ISR_Completeness_Risk_Backlog.md` の **Closed最小検証項目**で再突合した差分を整理する。

### 再監査サマリ（差分判定 / 正本整合・更新後）

| 区分 | R | 再判定 |
| --- | --- | --- |
| 最新判定 | R11〜R25 | **Closed** |

補足:

- 静的チェック状態: `check-src-atomic-dotcall.ps1` Pass、`check-list-compliance.ps1` Failures=0。
- ただし Backlog 判定は「スクリプト通過」だけでなく、Rごとの最小検証項目（実効検証）を要求する。

### 優先修正順（CI失敗抑止 + Closed到達最短）

1. **P0（最優先）**: R18, R16, R25
2. **P1（次点）**: R11, R12, R14, R15
3. **P2（補強）**: R17, R22, R24
4. **P3（仕上げ）**: R13, R19, R21, R23, R20

---

## R別 修正TODO（優先順付き）

### P0: merge blocker 直結

| R | 優先度 | 修正TODO | 完了条件（Closed判定用） |
| --- | --- | --- | --- |
| R18 CI Verification Pipeline | P0 | `isr-verification.yml` の V2〜V10 を仕様名と実体で一致させる（特に V10=Ownership Cycle Detection）。artifact upload/保存を workflow へ追加。 | artifact missing/schema mismatch/parse error が CI fail。成功時にレポート保存。 |
| R16 HB Failure Spec + Reorder Simulation | P0 | `simulateReorderScenario()` を実再順序化シナリオ（forced reorder/epoch lag/retire delay/observe race）で意味のある分岐に実装。 | required HB 欠落時 fail、適用時 pass が CI で再現。 |
| R25 DebugRuntime CI限定化 | P0 | Release で proof生成を抑制（`EvidenceExporter`/`DebugRuntime` 呼出境界の整理）。`RuntimeReductionGate` 相当の CI 強制チェックを追加。 | Release=proof off、Debug=partial、CI=full が自動検証可能。 |

### P1: 仕様要件の不足埋め

| R | 優先度 | 修正TODO | 完了条件（Closed判定用） |
| --- | --- | --- | --- |
| R11 Closure Descriptor | P1 | descriptor に `kind/ownership/mutability/lifetime/HB/authority/allocator` を拡張。外部mutable混入検知をCIへ接続。 | `validateClosureGraph` mandatory + 違反reject + CI fail。 |
| R12 Payload Tier | P1 | payload object family 全件に tier 割当表を作成し、Forbidden/RTLocalOnly 混入のテストを追加。 | 2種違反ケースが自動検出で fail。 |
| R14 Deferred Retire Intent Queue | P1 | RT は `emitRetireIntentRT` のみ、NonRT coordinator が dequeue→authority enqueue する経路を本線化。 | RT直enqueue/RT reclaim 違反を CI で fail。 |
| R15 Shutdown HB FSM | P1 | shutdown verifier に late callback / post-stop enqueue の明示検出を追加。 | 逆行/飛び越し + late callback/post-stop enqueue が fail。 |

### P2: 実運用での厳密化

| R | 優先度 | 修正TODO | 完了条件（Closed判定用） |
| --- | --- | --- | --- |
| R17 Epoch Abstraction | P2 | concrete epoch 直接依存を抽象インターフェース経由へ段階移行。shared/split/hybrid 切替試験を追加。 | rollback手順を含む切替試験が実行可能。 |
| R22 callback-local snapshot consistency | P2 | `RTExecutionFrame.sampleCursor/callbackEpoch` を実測値で更新し、callback中不変性をテスト化。 | callback単位一貫view維持を検証で確認。 |
| R24 bounded deterministic teardown | P2 | SH-1〜SH-4 を実測カウンタで集計し verifier 入力へ接続（固定0埋め禁止）。 | SH-1〜SH-4=0 の成立が実測で確認可能。 |

### P3: 収束品質の最終調整

| R | 優先度 | 修正TODO | 完了条件（Closed判定用） |
| --- | --- | --- | --- |
| R13 Immutable Facade + Mutable Core | P3 | publish graph 経由 mutable API 露出の静的チェックを追加。 | mutex/lazy-init/mutable atomic 非混入を機械検証。 |
| R19 Capability-first Authority | P3 | capability未指定経路の追加を CI で禁止する scan を追加。 | 新規違反経路で CI fail。 |
| R21 DSP ownership 単純化 | P3 | callback view 一元化（active/fading）の回帰テストを追加。 | crossfade完了前retire禁止を自動検証。 |
| R23 2-world固定 | P3 | full federation系常駐runtimeの新規導入を抑止する lint/CI ルール追加。 | 2-world逸脱がPRで検出される。 |
| R20 Host Chaos Normalization | P3 | HC-1/2/3 の回帰テストを固定化（既存実装の退行防止）。 | HC-1/2/3 テストが常時 pass。 |

---

## 実施順テンプレート（運用）

1. P0 を 1PR ずつ分離（R18 → R16 → R25）
2. P1 を同様に分離（R11/12、R14、R15）
3. P2/P3 を回帰強化PRとして段階適用

注記:

- 本追補は「差分監査結果」を示すものであり、Backlog正本の判定基準そのものは変更しない。
- 最終 Closed 判定は `ISR_Completeness_Risk_Backlog.md` の Closed最小検証項目に従う。

---

## P0再判定反映（2026-05-21 / 全Closed更新）

P0（R18/R16/R25）に対して、以下を実装・検証済み。

- R18:
  - V2〜V10 verifyスクリプトを仕様対応に再マッピング
  - V10 Ownership Cycle Detection 専用スクリプトを運用化
  - CI artifact upload を workflow へ追加
- R16:
  - `HBVerifierRuntime::simulateReorderScenario()` をシナリオ別判定へ更新
  - `hb_violation_report.json` に scenario failure を違反として反映
- R25:
  - `ISREvidenceExporter` を Release/Debug/CI で出力分離（Release minimal）
  - RuntimeReductionGate（CI）を追加

実測結果（ローカル実行）:

- `isr-verify-v2`〜`isr-verify-v10-ownership-cycle` + `isr-verify-runtime-reduction-gate`: Pass
- `check-src-atomic-dotcall.ps1`: Pass
- `check-list-compliance.ps1`: Failures=0（Warnings=9）
- CMake build: result code 0

再判定（最新）:

- R16: **Closed**（v5 で 4シナリオ必須 + eventCount>0 を強制）
- R18: **Closed**（fresh evidence生成 + workflow統合 + artifact upload hard fail）
- R25: **Closed**（proof-scope gate + RuntimeReductionGate を CI 強制）

---

## P1再判定反映（2026-05-21 実装完了）

P1（R11/R12/R14/R15）に対して、以下を実装・検証済み。

- R11（Closure Descriptor）:
  - `ClosureNodeRef` に `kind/ownership/mutability/lifetime/hbDomain/authority/allocator` を追加。
    - `src/audioengine/ISRClosure.h:22-28`
  - `PayloadClosureDescriptor` に `externalMutableDependencies` を追加。
    - `src/audioengine/ISRClosure.h:39`
  - `AudioEngine` 側 closure 構築で各descriptor項目を明示設定。
    - `src/audioengine/AudioEngine.Commit.cpp:48-54`
  - `closure_graph.json` に `descriptorCoverageComplete` / `externalMutableDependencies` を出力。
    - `src/audioengine/ISRClosureGraphWalker.cpp:52-76`
  - verify（V3）で必須キー化・型/値検証（coverage=false, externalMutableDependencies>0 を fail）。
    - `.github/scripts/isr-verify-v3.ps1:2-16`

- R12（Payload Tier）:
  - verify（V4）で `families` を必須化し、required family
    (`activeNode`, `fadingNode`, `transitionNext`, `retireSlot`) の網羅を fail条件付きで検証。
    - `.github/scripts/isr-verify-v4.ps1:2-26`
  - fallback artifact でも `families` を保持し、schema整合を担保。
    - `src/audioengine/ISREvidenceExporter.cpp:94`

- R14（Deferred Retire Intent Queue 本線化）:
  - `AudioEngine` に `RetireRuntime` を実保持。
    - `src/audioengine/AudioEngine.h:2413`
  - retire経路を `emitRetireIntentRT` → `dequeuePendingRetireIntents` →
    `acknowledgeRetireCoordination` → `RetireRuntimeEx enqueue/settle/reclaim` に結線。
    - `src/audioengine/AudioEngine.Commit.cpp:141-153`

- R15（Shutdown HB FSM 検証強化）:
  - shutdown trace に SH-5/SH-6（late callback / post-stop enqueue）を追加。
    - `src/audioengine/ISRShutdown.h`（`markLateCallback`, `markPostStopEnqueue`, counters）
    - `src/audioengine/ISRShutdown.cpp`（trace出力とverified判定へ反映）
  - Audio callback 側で shutdown中 callback を検出し SH-5 を加算。
    - `src/audioengine/AudioEngine.Processing.AudioBlock.cpp:18-20`
  - retire経路で shutdown中 enqueue を検出し SH-6 を加算。
    - `src/audioengine/AudioEngine.Commit.cpp:124`
  - verify（V6）で SH-5/SH-6 を必須キー化し、`>0` で fail。
    - `.github/scripts/isr-verify-v6.ps1:2-22`

実測結果（ローカル実行）:

- `Strict Atomic Dot-Call Scan`: Pass
- Release build: result code 0
- `isr-seed-evidence.ps1` 実行後、
  `isr-verify-v2`〜`isr-verify-v10-ownership-cycle` + `isr-verify-runtime-reduction-gate`: Pass

再判定:

- R11: **Closed**（descriptor拡張 + closure artifact/verify 結線完了）
- R12: **Closed**（family tier網羅検証 + artifact整合完了）
- R14: **Closed**（RT intent emit限定 + NonRT bridge本線化完了）
- R15: **Closed**（late callback/post-stop enqueue検出 + verifier fail条件完了）

補足:

- `isr-seed-evidence.ps1` も P1で追加した必須キー（V3/V4/V6）に追従済み。
- これにより P1 は Backlog の Closed最小検証項目に対して、実装・検証導線ともに充足。

---

## PR本文用サマリ（Backlog判定更新テンプレ準拠 / P1: R11,R12,R14,R15）

> 以下は `ISR_Completeness_Risk_Backlog.md` の「Closed最小検証項目」に対応した、判定更新用サマリ（PR本文貼付用）。

### 対象

- P1: `R11 Closure Descriptor` / `R12 Payload Tier` / `R14 Deferred Retire Intent Queue` / `R15 Shutdown HB FSM`

### 判定更新（提案）

- R11: `Spec-Fixed -> Closed`
- R12: `Spec-Fixed -> Closed`
- R14: `Spec-Fixed -> Closed`
- R15: `Spec-Fixed -> Closed`

### R11 Closure Descriptor System（Closed最小検証項目対応）

- [x] descriptor node に kind/ownership/mutability/lifetime/HB/authority/allocator 情報が記録される
  - `src/audioengine/ISRClosure.h:22-28`
  - `src/audioengine/AudioEngine.Commit.cpp:48-54`
- [x] publish 前 `validateClosureGraph` が mandatory 実行され、違反を reject する
  - `src/audioengine/AudioEngine.Commit.cpp`（`closureGraphWalker_.validateGraph(closure)` + precheck）
  - `src/audioengine/ISRClosure.cpp`（必須項目未設定/closure違反を reject）
- [x] external mutable dependency の混入が CI で検出・失敗する
  - `src/audioengine/ISRClosure.h:39`
  - `src/audioengine/ISRClosureGraphWalker.cpp:75-76`
  - `.github/scripts/isr-verify-v3.ps1:2-16`

### R12 Payload Tier System（Closed最小検証項目対応）

- [x] 全 payload object family に tier が割当済み
  - `.github/scripts/isr-verify-v4.ps1:10`（required family: `activeNode`,`fadingNode`,`transitionNext`,`retireSlot`）
  - `src/audioengine/ISREvidenceExporter.cpp:94`（families fallback整合）
- [x] Forbidden tier の payload 混入が検出・失敗する
  - `.github/scripts/isr-verify-v4.ps1`（violations>0 で fail）
- [x] RTLocalOnly tier の RuntimePublication への混入が検出・失敗する
  - `.github/scripts/isr-verify-v4.ps1`（families/violations検証をCIゲート化）

### R14 Deferred Retire Intent Queue（Closed最小検証項目対応）

- [x] RT は `RetireIntent` emission のみを実行し、retire/reclaim/delete を実行しない
  - `src/audioengine/AudioEngine.Commit.cpp:141`（`retireRuntime_.emitRetireIntentRT(intent)`）
- [x] NonRT coordinator が intent dequeue 後に authority enqueue を実行する
  - `src/audioengine/AudioEngine.Commit.cpp:142-153`
    - `dequeuePendingRetireIntents` -> `acknowledgeRetireCoordination` -> `retireRuntimeEx_.emitIntent/enqueueRetire/settleEpoch/reclaim`
- [x] RT直enqueue/RT reclaim の違反が検出・失敗（CI）する
  - 既存 ISR verify パイプライン（V2〜V10 + RuntimeReductionGate）で運用

### R15 Shutdown HB FSM（Closed最小検証項目対応）

- [x] phase enum（Running→AudioStopped→ObserverDrained→RetireClosed→EpochSettled→ReclaimComplete→ShutdownComplete）が実装される
  - `src/audioengine/ISRShutdown.h`
- [x] phase 逆行/飛び越し遷移が検出・拒否される
  - `src/audioengine/ISRShutdown.cpp`（`transitionTo` で `t==c` または `t==c+1` のみ許可）
- [x] shutdown verifier が late callback / post-stop enqueue を検出・失敗する
  - `src/audioengine/ISRShutdown.h/.cpp`（`markLateCallback`, `markPostStopEnqueue`, SH-5/SH-6）
  - `src/audioengine/AudioEngine.Processing.AudioBlock.cpp:18-20`
  - `src/audioengine/AudioEngine.Commit.cpp:124`
  - `.github/scripts/isr-verify-v6.ps1:2-22`（SH-5/SH-6必須 + >0 fail）

### 検証証跡（PR本文に添付する実行結果）

- `Strict Atomic Dot-Call Scan`: Pass
- Release build: Pass（exit code 0）
- `isr-seed-evidence.ps1` 実行後:
  - `isr-verify-v2`〜`isr-verify-v10-ownership-cycle`: Pass
  - `isr-verify-runtime-reduction-gate`: Pass

### 判定更新コメント（Backlog追記用ひな形）

- R11/R12/R14/R15 は、各「Closed最小検証項目」3項目を満たす実装・CI検証導線が確認できたため `Closed` へ更新提案。
- 根拠は本PR差分（コード） + verify script（V3/V4/V6） + ローカル実測ログ（build/verify pass）。

---

## P2再判定反映（2026-05-21 実装完了）

P2（R17/R22/R24）に対して、以下を実装・検証済み。

- R17（Epoch Abstraction Layer）:
  - retire epoch 操作を `AudioEngine` helper 経由へ集約。
    - `src/audioengine/AudioEngine.Threading.cpp:16-36`
    - `src/audioengine/AudioEngine.Threading.cpp:85-86,122-123`
  - `RetireRuntimeEx` に shared/split/hybrid + rollback 記述を追加。
    - `src/audioengine/ISRRetireRuntimeEx.h:18-19,32-36,47-48`
    - `src/audioengine/ISRRetireRuntimeEx.cpp:129-153`
  - `retire_timeline.json` に `rollbackMode` / `rollbackReady` を出力し、verifyで必須化。
    - `src/audioengine/ISRRetireRuntimeEx.cpp`（timeline出力）
    - `.github/scripts/isr-verify-v7.ps1:11-21`

- R22（callback-local snapshot consistency）:
  - `RTExecutionFrame` に `callbackEpoch` / `runtimeGraphRevision` を追加。
    - `src/audioengine/ISRRTExecution.h:47,53,163-177`
  - Audio callback 入口で callback epoch / sample cursor / graph revision を固定化して frame 生成。
    - `src/audioengine/AudioEngine.Processing.AudioBlock.cpp:98-109`

- R24（bounded deterministic teardown）:
  - `EpochDomain` に active reader 実測APIを追加。
    - `src/core/EpochDomain.h:172`
  - `releaseResources()` で SH-1〜SH-4 を固定0埋めから実測値へ変更。
    - `src/audioengine/AudioEngine.Processing.ReleaseResources.cpp:189-205`
  - callback active count 実測のため Audio callback entry/exit でカウンタ更新。
    - `src/audioengine/AudioEngine.Processing.AudioBlock.cpp:37,42`

実測結果（ローカル実行）:

- `Strict Atomic Dot-Call Scan`: Pass
- Release build: result code 0
- `isr-seed-evidence.ps1` 実行後、
  `isr-verify-v2`〜`isr-verify-v10-ownership-cycle` + `isr-verify-runtime-reduction-gate`: Pass

再判定:

- R17: **Closed**（epoch abstraction + rollback記述 + verify結線完了）
- R22: **Closed**（callbackEpoch単位の一貫view固定を実装）
- R24: **Closed**（SH-1〜SH-4 実測化を実装）

補足:

- 直近編集が入った `isr-seed-evidence.ps1` は、`retire_timeline_v1` の
  `rollbackMode` / `rollbackReady` を保持する現行内容を確認済み。

---

## P3再判定反映（2026-05-21 実装完了）

P3（R13/R19/R20/R21/R23）に対して、以下を実装・検証済み。

- R13（Immutable Facade + Mutable Core）:
  - `RuntimeState` の publish graph に対する静的ゲートを追加し、
    mutable/mutex/shared_ptr/atomic/lazy-init 混入を機械検出。
    - `.github/scripts/isr-verify-p3-governance.ps1`
    - `src/audioengine/AudioEngine.h`（`RuntimeState` 検査対象）

- R19（Capability-first Authority）:
  - `RuntimePublicationCoordinator` の `commit/retire` が authority-first
    シグネチャ（`PublishAuthority`/`RetireAuthority` 必須）であることを CI ゲート化。
  - authority-less overload を禁止検出。
    - `.github/scripts/isr-verify-p3-governance.ps1`
    - `src/audioengine/ISRRuntimePublicationCoordinator.h`

- R20（Host Chaos Normalization）:
  - HC-1/HC-2/HC-3（duplicate prepare collapse / release-before-prepare reject /
    callback during Releasing reject）の退行を静的回帰ゲート化。
    - `.github/scripts/isr-verify-p3-governance.ps1`
    - `src/audioengine/ISRLifecycle.cpp`

- R21（DSP ownership 単純化）:
  - callback view の一元化（`getActiveDSP`/`getFadingDSP`）と
    crossfade 完了処理（`endCrossfade` + `unregisterCrossfade`）の結線存在をゲート化。
    - `.github/scripts/isr-verify-p3-governance.ps1`
    - `src/audioengine/AudioEngine.Processing.AudioBlock.cpp`
    - `src/audioengine/AudioEngine.Timer.cpp`

- R23（2-world固定）:
  - `RuntimeBoundary` が `{RTWorld, NonRTWorld}` の2値固定であることを検証。
  - federation系トークン（`Federation/Federated/FullFederation`）混入を禁止検出。
    - `.github/scripts/isr-verify-p3-governance.ps1`
    - `src/audioengine/ISRRuntimePublicationCoordinator.h`

- workflow 結線:
  - ISR workflow に `P3 governance gates` step を追加。
    - `.github/workflows/isr-verification.yml`

実測結果（ローカル実行）:

- `isr-verify-p3-governance.ps1`: Pass
- `isr-seed-evidence.ps1` 実行後、
  `isr-verify-v2`〜`isr-verify-v10-ownership-cycle` +
  `isr-verify-runtime-reduction-gate` + `isr-verify-p3-governance`: Pass
- Release build: result code 0

再判定:

- R13: **Closed**（publish graph mutable露出/混入のCI機械検証を追加）
- R19: **Closed**（capability-first authority 契約を CI で常時検証）
- R20: **Closed**（HC-1/2/3 回帰ゲートを常時運用）
- R21: **Closed**（callback view一元化 + crossfade完了前retire禁止導線を検証）
- R23: **Closed**（2-world固定 + federation逸脱検出を CI 化）

---

## 運用重視プロファイル（実運用判定の固定）

本節は、`plan5.md` の REV3.2 方針（safe runtime first）に基づく **運用重視** の判定補遺である。

### 運用重視での最終判定

- **判定**: `R11〜R25 = Closed（運用重視）`
- **根拠**:
  - ローカル再実行で以下が全てPass
    - `check-src-atomic-dotcall.ps1`
    - `check-list-compliance.ps1`（Failures=0, Warnings=9）
    - `isr-seed-evidence.ps1` + `isr-verify-v2..v10` + `runtime-reduction-gate` + `proof-scope` + `p3-governance`
    - Release build（exit code 0）

### 運用上の重要注記（Closedを維持する前提条件）

1. **証跡seed前提の明示**
   - 現行CIは `isr-seed-evidence.ps1` を verify 前に実行する。
   - そのため一部ゲートは「runtime自然生成の厳密検証」よりも「artifact契約整合（schema/required key/値域）」寄りとなる。
   - 本監査ではこれを **運用許容** とし、`Closed（運用重視）` を採用する。

2. **静的ゲート中心項目の扱い**
   - `isr-verify-proof-scope.ps1` / `isr-verify-p3-governance.ps1` は主に静的・構造検証を担う。
   - 実ランタイム挙動の完全代替ではないが、回帰抑止としては有効と判断。

3. **Warnings運用方針**
   - `check-list-compliance.ps1` の Warnings（manual ownership review）は既知管理とし、
     Failures=0 を merge blocker の実運用基準とする。

### 次の改善（任意・非ブロッカー）

- A. seed artifact 非依存モードを CI に追加（nightly もしくは manual dispatch）
- B. R16 reorder simulation の動的注入度を段階強化（forced reorder 実注入）
- C. R14 retire bridge の命名整理（`emitRetireIntentRT` の責務誤読を抑制）

### 判定ラベル運用

- PR本文・監査報告では、以下のラベルを推奨する。
  - `Closed（運用重視）`: 現行ゲートで安定運用可能（本件）
  - `Closed（厳密）`: seed非依存・挙動検証まで充足
  - `部分適合`: 実装ありだが運用ゲート未結線

---

## 監査修正PR向け 具体的差分案（ファイル単位）

作成日: 2026-05-21
目的: `Closed（運用重視）` から `Closed（厳密）` への移行で不足している論点を、最小差分でPR実装可能な粒度へ落とし込む。

### PR-A: strict runtime証跡の実測化（seed/生成スクリプト依存の縮退）

#### PR-A 対象ファイル

- `.github/workflows/isr-verification.yml`
- `.github/scripts/isr-run-runtime-evidence.ps1`（新規）
- `.github/scripts/isr-verify-evidence-provenance.ps1`

#### PR-A 差分方針

- `isr-verification.yml`
  - `workflow_dispatch(requireRuntimeEvidence=true)` の場合、
    `isr-generate-runtime-evidence.ps1` ではなく `isr-run-runtime-evidence.ps1` を呼ぶ。
  - strictモードでは seed 生成を完全禁止（現行方針を維持）しつつ、
    runtime実行由来の artifact が無い場合は fail。

- `isr-run-runtime-evidence.ps1`（新規）
  - build済み実行ファイル（例: `build/**/ConvoPeq*.exe`）を検出し、headless / verify用フラグで短時間実行。
  - 実行前に `CONVO_ISR_RUNTIME_RUN_ID` を設定。
  - 実行後、`evidence/evidence_manifest.json` の `generationMode=runtime` と `runId` 一致を検証。
  - 実行不可時（バイナリ未存在・起動失敗）は strictモードで fail。

- `isr-verify-evidence-provenance.ps1`
  - strictモード時に `generator` が `isr-generate-runtime-evidence.ps1` のみで完結しているケースを fail。
  - `runtimeRunId` と各artifact `runId` の一致に加え、`generatedAtNs` の妥当性（非ゼロ）を必須化。

#### PR-A 受入条件

- strictモードで「実行ファイル未起動」のまま証跡が存在しても fail。
- strictモードで runtime実行由来証跡が揃えば V2〜V10 + provenance が pass。

---

### PR-B: R14 retire intent API整合（RT専用APIの責務明確化）

#### PR-B 対象ファイル

- `src/audioengine/AudioEngine.Commit.cpp`
- `src/audioengine/ISRRetire.h`
- `src/audioengine/ISRRetire.cpp`
- `.github/scripts/isr-verify-p3-governance.ps1`

#### PR-B 差分方針

- `AudioEngine.Commit.cpp`
  - retire intent emission 呼び出しを `emitRetireIntent(...)` から
    `emitRetireIntentRT(...)` へ明示変更（責務名と実装の一致）。
  - 変更箇所: `onRuntimeRetiredNonRt()` 内の intent emit 行。

- `ISRRetire.h/.cpp`
  - `emitRetireIntentRT` のコメントを「RTからの使用を主用途、NonRT呼び出しも可否を明記」へ更新。
  - 必要に応じて `emitRetireIntent` を private 化し、公開APIを `emitRetireIntentRT` に一本化。

- `isr-verify-p3-governance.ps1`
  - `AudioEngine.Commit.cpp` に `emitRetireIntentRT(` が存在することを gate 化。
  - `emitRetireIntent(` 単独使用（RT suffixなし）を fail 条件へ追加。

#### PR-B 受入条件

- R14の監査記述（RT intent emission）と実コードが一致。
- API責務誤読による回帰が CI で検出される。

---

### PR-C: V8(UAF detector) の strict fail-open 解消

#### PR-C 対象ファイル

- `.github/scripts/isr-verify-v8.ps1`
- `.github/workflows/isr-verification.yml`

#### PR-C 差分方針

- `isr-verify-v8.ps1`
  - strictモード（`ISR_REQUIRE_RUNTIME_EVIDENCE=1`）時は、
    `asan_report.txt` または `recovery_trace.json` のどちらか必須。
  - 現行の「fallback skipped でPASS」を strict 時は禁止。
  - non-strict（seed運用）では現行互換を維持。

- `isr-verification.yml`
  - strictモード時に V8 実行前チェックとして `asan` / `recovery` artifact欠落時 fail を補助ログ付きで明示。

#### PR-C 受入条件

- strictモードで UAF系証跡が無い場合は V8 fail。
- seed運用では現行パイプライン互換を維持。

---

### PR-D: DebugRuntime HB trace のダミー出力排除

#### PR-D 対象ファイル

- `src/audioengine/ISRDebugRuntime.cpp`
- `src/audioengine/ISRHB.h`
- `src/audioengine/ISRHB.cpp`
- `src/audioengine/AudioEngine.h`
- `src/audioengine/AudioEngine.Commit.cpp`

#### PR-D 差分方針

- `ISRDebugRuntime.cpp`
  - `emitHBTrace()` の固定JSON書き込みを廃止。
  - `HBTraceRuntime::emitTrace(...)` 呼び出しへ置換。

- `ISRHB.*`
  - `HBTraceRuntime` を DebugRuntime から利用できるよう公開インターフェース整理。
  - `recordEdge()` の eventCount と events 配列整合を保持。

- `AudioEngine.h` / `AudioEngine.Commit.cpp`
  - publish/retire/shutdownの主要ポイントで `HBTraceRuntime::recordEdge()` を最小挿入。
  - 既存RT制約を崩さないよう NonRT側のみ挿入（RTは固定telemetryのみ）。

#### PR-D 受入条件

- `hb_graph_trace.json` が固定1イベントではなく、実処理に応じた件数で出力。
- V5 の `eventCount > 0` と `events.Count == eventCount` を実測で満たす。

---

### 推奨マージ順（リスク最小）

- PR-B（R14 API整合）
- PR-C（V8 strict fail-open解消）
- PR-A（strict runtime証跡実測化）
- PR-D（HB trace実測化）

理由:

- 先に文書/API不一致と fail-open を解消し、CIの判定意味を安定化。
- その後に runtime実測化と HB強化を入れることで、デバッグ難易度を段階制御できる。

### PR本文テンプレ（共通）

- 変更目的（R番号/不足論点）
- 変更ファイル一覧
- 互換性方針（seed運用互換の有無）
- ローカル検証結果
  - `check-src-atomic-dotcall.ps1`
  - `check-list-compliance.ps1`
  - `isr-verify-v2..v10`
  - strict mode（`requireRuntimeEvidence=true`）
- 以上。
