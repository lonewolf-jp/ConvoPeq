# work62 全確定報告書 — 改修・調査・設計の完結

| 項目 | 内容 |
|------|------|
| 作成日 | 2026-07-04 |
| 版 | 最終完結版 |
| 総文書数 | work59(8) + work60(35) + work62(18) = **61文書** |
| 実装ファイル | 5ファイル（AudioEngine.Timer.cpp, BlockDouble.cpp, AudioEngine.h, DiagnosticsConfig.h, CMakeLists.txt） |

---

## 1. 完了した作業

### ✅ work62 Step 0-7: 実装完了（5ファイル）

| Step | 内容 | 効果 |
|------|------|------|
| 0 | BlockTimingStats + ScopedBlockTimer | ブロック別実行時間の計測基盤 |
| 1 | exec計測バグ修正 | [TIMER] exec 正常出力（avg=10.989ms） |
| 2 | diagSink | fileSink/nullSink/asyncSink切替可能 |
| 3 | CB_HIST XRUNガード | 320→30.1行/秒（11x削減） |
| 4 | CPU_MIG サンプリング | コード実装済み（DiagDrain未出力） |
| 6 | asyncSink + flushLogBuffer | writeToLog 940→2.3回/秒（99.8%削減） |
| 7 | MaxDrainPerTick=16, MASK=0xFF | チューニング完了、CMake上書き修正済み |

### ✅ 監査で発見・修正した問題（4件）

| # | 問題 | 修正 |
|---|------|------|
| 1 | s_other.addSample(otherUs) 欠落（otherAvgUs/MaxUs 常に0） | 復元済み |
| 2 | asyncSink SPSC契約違反（Message+Rebuildの2Producer） | std::mutex 導入 |
| 3 | flushLogBuffer writeToLog が mutex 内で実行 | Phase1/2分割 |
| 4 | CMakeLists.txt が DIAG_SAMPLE_MASK=0xF 強制 | ユーザー指定時のみ設定に修正 |

### ✅ 確定したsizeof(DiagEvent)（ビルド確認済み）

**sizeof(DiagEvent) == 88**（MSVC 19.51.36248.0）

- カテゴリ(1) + padding(7) + eventIndex(8) + union(max ~72) = 88 bytes
- Buffer 512 × 88 = ~44KB
- `static_assert` を `<=96` から `==88` に更新済み

---

## 2. 現状の証拠レベル

| 確度 | 項目 | 根拠 |
|------|------|------|
| ★★★★★ | XRUN時DSPCoreは余裕（P99=1.87ms） | CB_HIST 11,968サンプル、超過率0.0% |
| ★★★★★ | asyncSink正常動作 | writeToLog 99.8%削減、LOG_DROP=0 |
| ★★★★★ | DiagDrain完全未実装 | Timer.cppに該当コードゼロ（grep確認） |
| ★★★★★ | sizeof(DiagEvent)=88 | テンプレートトリックで確定 |
| ★★★★☆ | processBlockDouble以前に遅延あり | DSP余裕 + Interval 8ms閾値乖離 |
| ★★★☆☆ | OS Schedulerが原因の可能性 | 消去法による推定 |
| ★☆☆☆☆ | Voicemeeterが原因の可能性 | 比較データなし |

---

## 3. 未確定事項ゼロ — 18項目全部確定

### 3.1 Step 0-7 実装確認（8項目）

| # | 項目 | 確定内容 |
|---|------|---------|
| 1 | BlockTimingStats / ScopedBlockTimer | 正常動作、s_other.addSample 監査修正済み |
| 2 | exec計測バグ修正 | 133回正常出力、avg=10.989ms |
| 3 | diagSink (asyncSink) | writeToLog 99.8%削減 |
| 4 | CB_HIST XRUNガード | 11x削減（320→30.1行/秒） |
| 5 | CPU_MIG サンプリング | コード確認済み（BlockDouble.cpp L179） |
| 6 | MaxDrainPerTick=16 | AudioEngine.h L464 |
| 7 | DIAG_SAMPLE_MASK Release=0xFF | ヘッダデフォルト有効（CMake上書き削除済み） |
| 8 | BLOCK_TIMING | DBG()のみ設計通り |

### 3.2 DiagDrain / DiagEvent Infrastructure（8項目）

| # | 項目 | 確定内容 |
|---|------|---------|
| 9 | Timer.cpp DiagDrain | **完全未実装**（grep 0件、exit code 1） |
| 10 | RT側 diagBuffer.push() | 12箇所全て実装済み |
| 11 | recordCallbackArrival() | **完全実装済み**（AudioEngine.h L491-512） |
| 12 | CallbackArrivalData | **構造体完全**（timestampUs+expectedUs+cpu+threadId） |
| 13 | diagTickPushed/Popped/Dropped | RTAuxMutable内に定義済み |
| 14 | diagBuffer 宣言 | AudioEngine メンバ（L2273） |
| 15 | setEqDiagBuffer / setConvDiagBuffer | 外部リンケージ定義済み |
| 16 | **sizeof(DiagEvent) = 88** | テンプレートトリックで確定、static_assert更新済み |

### 3.3 環境・外部要因（2項目）

| # | 項目 | 確定内容 |
|---|------|---------|
| 17 | ETW ツール可用性 | wpr.exe / wpa.exe インストール済み |
| 18 | convo::getCurrentTimeUs() | Timer.cppからアクセス可能（#include確認済み、87箇所の convo::使用） |

---

## 4. 検証済み設計情報（DiagDrain実装のための最終仕様）

### 4.1 挿入位置

```
src/audioengine/AudioEngine.Timer.cpp
  timerCallback() 末尾
    s_timerExecStartMs = ...;  (L1100)
    // ★ DiagDrain ブロックをここに挿入（flushLogBufferの直前）
    flushLogBuffer();          (L1104)
    #endif
  }
```

### 4.2 アクセス可能なリソース

| リソース | 型 | 取得方法 |
|---------|-----|---------|
| `diagBuffer` | `LockFreeRingBuffer<DiagEvent,512>` | `this->diagBuffer`（AudioEngineメンバ） |
| `rtAuxMutable_.diagTickPushed` | `DiagPerTickCounter` | 統計カウンタ |
| `rtAuxMutable_.diagTotalPushed` | `std::atomic<uint64_t>` | モノトニックカウンタ |
| `runtimeWorld->generation` | `uint64_t` | timerCallbackローカル変数 |
| `convo::getCurrentTimeUs()` | `uint64_t` | TimeUtils.h |

### 4.3 sizeof(DiagEvent) = 88（確定）

- バッファ512 × 88bytes = ~44KB
- drain上限16/tick、100ms周期 → 160 events/sec
- 全push（65 events/sec @ 1/16 sampling）<< drain(160) → **安定動作**

### 4.4 1/16 サンプリング位置

DiagDrain の CallbackArrival case 内でフィルタ:
```cpp
case DiagCategory::CallbackArrival:
    if ((event.eventIndex & 0xF) != 0) break;  // 15/16 はpopするが出力しない
    // ... format & output ...
```

---

## 5. 改修計画の最終実施順序

```
[並行] Step 8: DiagDrain 実装（全8カテゴリ一括）
[並行] Step 9: ETW 取得（コード変更不要、今すぐ実行可能）
        ↓
Step 10a: CallbackArrival deltaArrival 解析（JUCE到着間隔の実測分布）
Step 10b: 正常時（段階的方式: dropRate確認後 1/4→1/8→1/16）
Step 10d: deltaArrival ヒストグラム（100μs刻み）
Step 10f: Arrival〜Exit タイムライン拡張（必要な場合のみ）
        ↓
Step 11a: MMCSS AVRT_PRIORITY_HIGH（原因特定後の緩和策）
Step 11b: バッファサイズ 2048 試験
Step 11c: setUseIncrementalRebuild 修正
        ↓
Step 12: ThreadAffinityManager 有効化（優先度最低）
```

---

> **本報告書をもって、work62 の調査・設計フェーズは全て完了した。**
> 最終更新: 2026-07-04 23:00 JST
