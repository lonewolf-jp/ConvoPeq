{
    "servers": {
        "codegraph": {
            "type": "stdio",
            "command": "${workspaceFolder}\\.venv-codegraph\\Scripts\\codegraph-mcp.exe",
            "args": [
                "serve",
                "--repo",
                "${workspaceFolder}"
            ]
        }
    }
}